# Auto-start tmux in interactive shells when not already inside tmux.
if command -v tmux >/dev/null 2>&1; then
  if [ -n "$PS1" ] && [ -z "$TMUX" ] && [ "${TERM_PROGRAM}" != "vscode" ]; then
    tmux attach -t dev 2>/dev/null || tmux new -s dev
  fi
fi
